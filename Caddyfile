:8080 {
    log {
        output stdout
    }

    # Set the PostgREST URL from the environment variable
    reverse_proxy {env.POSTGREST_URL}

    # Test endpoint
    handle /test {
        respond "Caddy is working!" 200
    }
}