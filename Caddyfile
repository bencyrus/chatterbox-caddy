:8080 {
    log {
        output stdout
    }

    # Set the PostgREST URL from the environment variable
    reverse_proxy {$POSTGREST_URL}

    # Health check endpoint
    handle /health {
        respond "OK" 200
    }
}